# Генерички интерфејси

Генерички интерфејси (енгл. *generic interfaces*) омогућавају дефинисање
интерфејса са типским параметрима.

## Дефиниција генеричког интерфејса

Генерички интерфејс се дефинише додавањем типског параметра `<T>` након имена
интерфејса. У следећем примеру...

```cs

```
